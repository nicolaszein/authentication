try:
    from setuptools import setup
except ImportError:
    from distutils.core import setup


setup(
    name='authentication',
    packages=[''],
    version='0.0.1',
    description="",
    long_description="",
    license='',
    include_package_data=True,
    tests_require=[],
)
